{
  "name": "@tokengate/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "tokengate": "./src/index.ts"
  },
  "scripts": {
    "dev": "dotenv -e ../../.env.local -- bun run src/index.ts",
    "check": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@tokengate/crypto": "workspace:*",
    "@tokengate/env-format": "workspace:*",
    "@tokengate/sdk": "workspace:*",
    "dotenv-cli": "^8.0.0"
  }
}
