{
  "name": "@tokengate/convex",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bunx convex dev --env-file .env.local",
    "check": "bunx convex dev --once --env-file .env.local"
  },
  "dependencies": {
    "convex": "^1.18.2"
  }
}
